services:
  - type: web
    name: jse-pairs-api
    runtime: python
    repo: https://github.com/AltafWilkens/LIVE_PAIRS_TRADING_DASHBOARD.git
    plan: free
    buildCommand: pip install -r requirements-api.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000